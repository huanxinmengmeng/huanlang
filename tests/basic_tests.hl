# 基础语法测试
# 测试基础语法示例的功能

导入 幻语.测试 为 测试

# 测试变量示例
测试组 变量测试
    测试 测试变量声明()
    开始
        令 年龄 类型 整数 为 25
        令 温度 类型 浮点64 为 36.5
        令 姓名 类型 字符串 为 "张三"
        令 是否成年 类型 布尔 为 真
        
        测试.断言(年龄 等于 25, "年龄应该是25")
        测试.断言(温度 接近 36.5, 0.001, "温度应该是36.5")
        测试.断言(姓名 等于 "张三", "姓名应该是张三")
        测试.断言(是否成年 等于 真, "是否成年应该是真")
    结束
    
    测试 测试类型推导()
    开始
        令 分数 为 95
        令 圆周率 为 3.14159
        令 消息 为 "成功"
        
        测试.断言(分数 类型 是 整数, "分数应该是整数类型")
        测试.断言(圆周率 类型 是 浮点64, "圆周率应该是浮点64类型")
        测试.断言(消息 类型 是 字符串, "消息应该是字符串类型")
    结束
结束

# 测试控制流示例
测试组 控制流测试
    测试 测试判断等级()
    开始
        函数 判断等级(分数 类型 整数) 返回 字符串
        开始
            若 分数 大于等于 90 则
                返回 "优秀"
            否则若 分数 大于等于 80 则
                返回 "良好"
            否则若 分数 大于等于 60 则
                返回 "及格"
            否则
                返回 "不及格"
            结束
        结束
        
        测试.断言(判断等级(95) 等于 "优秀", "95分应该是优秀")
        测试.断言(判断等级(85) 等于 "良好", "85分应该是良好")
        测试.断言(判断等级(65) 等于 "及格", "65分应该是及格")
        测试.断言(判断等级(55) 等于 "不及格", "55分应该是不及格")
    结束
    
    测试 测试模式匹配()
    开始
        函数 演示模式匹配(值 类型 整数) 返回 字符串
        开始
            匹配 值
                当 0 => 返回 "零"
                当 1 => 返回 "一"
                当 2 => 返回 "二"
                当 3..=5 => 返回 "三到五"
                默认 => 返回 "其他"
            结束
        结束
        
        测试.断言(演示模式匹配(0) 等于 "零", "0应该匹配零")
        测试.断言(演示模式匹配(1) 等于 "一", "1应该匹配一")
        测试.断言(演示模式匹配(2) 等于 "二", "2应该匹配二")
        测试.断言(演示模式匹配(4) 等于 "三到五", "4应该匹配三到五")
        测试.断言(演示模式匹配(10) 等于 "其他", "10应该匹配其他")
    结束
end

# 运行所有测试
函数 主() 返回 整数
开始
    测试.运行所有测试()
    return 0
结束
