# 函数测试
# 测试函数和方法示例的功能

导入 幻语.测试 为 测试

# 测试基本函数
测试组 函数测试
    测试 测试相加函数()
    开始
        函数 相加(甲 类型 整数, 乙 类型 整数) 返回 整数
        开始
            return 甲 + 乙
        结束
        
        测试.断言(相加(3, 5) 等于 8, "3 + 5 应该等于 8")
        测试.断言(相加(-2, 7) 等于 5, "-2 + 7 应该等于 5")
    结束
    
    测试 测试阶乘函数()
    开始
        函数 阶乘(数值 类型 整数) 返回 整数
        开始
            若 数值 小于等于 1 则
                return 1
            结束
            return 数值 * 阶乘(数值 - 1)
        结束
        
        测试.断言(阶乘(0) 等于 1, "0! 应该等于 1")
        测试.断言(阶乘(1) 等于 1, "1! 应该等于 1")
        测试.断言(阶乘(5) 等于 120, "5! 应该等于 120")
    结束
    
    测试 测试泛型交换函数()
    开始
        函数 交换<类型名>(甲 类型 借用 可变 类型名, 乙 类型 借用 可变 类型名)
        开始
            令 临时 为 甲
            甲 设为 乙
            乙 设为 临时
        结束
        
        令 a 为 10
        令 b 为 20
        交换(借用 可变 a, 借用 可变 b)
        测试.断言(a 等于 20, "a 应该等于 20")
        测试.断言(b 等于 10, "b 应该等于 10")
        
        令 x 为 "hello"
        令 y 为 "world"
        交换(借用 可变 x, 借用 可变 y)
        测试.断言(x 等于 "world", "x 应该等于 world")
        测试.断言(y 等于 "hello", "y 应该等于 hello")
    结束
    
    测试 测试高阶函数()
    开始
        函数 应用两次<输入类型, 输出类型>(
            函数 类型 函数(输入类型) 返回 输出类型,
            值 类型 输入类型
        ) 返回 输出类型
        开始
            return 函数(函数(值))
        结束
        
        令 加倍 为 |值| { 值 * 2 }
        测试.断言(应用两次(加倍, 3) 等于 12, "应用两次加倍应该等于 12")
    结束
    
    测试 测试闭包()
    开始
        令 平方 为 |值| { 值 * 值 }
        测试.断言(平方(5) 等于 25, "5的平方应该等于25")
        
        令 列表 为 [1, 2, 3, 4, 5]
        令 平方列表 为 列表.映射(|值| { 值 * 值 })
        测试.断言(平方列表 等于 [1, 4, 9, 16, 25], "平方列表应该正确")
        
        令 偶数列表 为 列表.过滤(|值| { 值 % 2 == 0 })
        测试.断言(偶数列表 等于 [2, 4], "偶数列表应该正确")
        
        令 总和 为 列表.折叠(0, |累加, 值| { 累加 + 值 })
        测试.断言(总和 等于 15, "总和应该等于15")
    结束
end

# 运行所有测试
函数 主() 返回 整数
开始
    测试.运行所有测试()
    return 0
结束
