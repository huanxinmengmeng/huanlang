# 幻语编程语言 - 开发规范文档
# 11. 后端代码生成接口

**版本**: v0.4.0
**许可证**: Apache-2.0
**更新日期**: 2026年5月
**适用对象**: 后端开发者、平台移植工程师

> **说明**: 本章详细描述幻语编译器的后端代码生成接口，包括LLVM后端、WebAssembly后端和GPU后端的实现规范。

---

## 11.1 概述

后端代码生成器负责将经过语义分析和 MLIR 降级后的中间表示转换为特定目标平台的机器码、汇编代码或字节码。幻语编译器采用模块化后端设计，支持多种目标平台：

- **LLVM 后端**：生成 x86、ARM、RISC‑V 等架构的原生机器码，是幻语的主要代码生成路径。
- **WebAssembly 后端**：生成 Wasm 字节码，用于浏览器和边缘计算环境。
- **GPU 后端**：生成 CUDA、HIP、OpenCL 等 GPU 内核代码，支持并行计算加速。

所有后端实现统一的 `CodeGenerator` 接口，通过命令行参数 `--target` 选择具体后端。后端模块位于 `src/core/backend/` 目录下，由 Rust 实现。

## 11.2 统一后端接口

### 11.2.1 CodeGenerator Trait

```rust
/// 代码生成器统一接口
pub trait CodeGenerator {
    /// 创建代码生成器实例
    fn new(target: TargetTriple, options: CodeGenOptions) -> Self;

    /// 从 MLIR 模块生成目标代码（二进制格式）
    fn generate(&mut self, mlir_module: &mlir::ModuleOp) -> Result<Vec<u8>, CodeGenError>;

    /// 生成汇编代码文本
    fn emit_assembly(&mut self, mlir_module: &mlir::ModuleOp) -> Result<String, CodeGenError>;

    /// 生成 LLVM IR 文本（仅 LLVM 后端支持）
    fn emit_llvm_ir(&mut self, mlir_module: &mlir::ModuleOp) -> Result<String, CodeGenError> {
        Err(CodeGenError::Unsupported("LLVM IR 文本生成不支持此目标".into()))
    }

    /// 链接多个目标文件，生成最终可执行文件或库
    fn link(&self, objects: Vec<PathBuf>, output: PathBuf) -> Result<(), LinkError>;
}
```

### 11.2.2 代码生成选项

```rust
#[derive(Debug, Clone)]
pub struct CodeGenOptions {
    /// 优化级别：0-3, 's' (尺寸), 'z' (激进尺寸)
    pub opt_level: OptLevel,
    /// 是否生成调试信息
    pub debug_info: bool,
    /// 是否生成位置无关代码（PIC/PIE）
    pub pic: bool,
    /// 是否启用栈保护
    pub stack_protector: bool,
    /// 目标 CPU 特性（如 "+avx2,+fma"）
    pub target_features: String,
    /// 调用约定
    pub calling_convention: CallingConvention,
    /// 是否保留内联汇编
    pub keep_asm: bool,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum OptLevel {
    None = 0,
    Less = 1,
    Default = 2,
    Aggressive = 3,
    Size = b's' as isize,
    SizeAggressive = b'z' as isize,
}

#[derive(Debug, Clone)]
pub enum CallingConvention {
    HuanDefault,
    C,
    Fast,
    Interrupt,
}
```

### 11.2.3 目标三元组

```rust
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct TargetTriple(String);

impl TargetTriple {
    pub fn host() -> Self {
        // 根据当前编译目标返回平台三元组
        Self(std::env::consts::ARCH.to_string() + "-unknown-" + std::env::consts::OS)
    }

    pub fn as_str(&self) -> &str { &self.0 }

    pub fn arch(&self) -> Arch {
        if self.0.starts_with("x86_64") { Arch::X86_64 }
        else if self.0.starts_with("aarch64") { Arch::AArch64 }
        else if self.0.starts_with("arm") || self.0.starts_with("thumb") { Arch::ARM }
        else if self.0.starts_with("riscv32") { Arch::RiscV32 }
        else if self.0.starts_with("riscv64") { Arch::RiscV64 }
        else if self.0.starts_with("wasm32") { Arch::Wasm32 }
        else { Arch::Unknown }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum Arch {
    X86_64, AArch64, ARM, RiscV32, RiscV64, Wasm32, Unknown,
}
```

### 11.2.4 错误类型

```rust
#[derive(Debug, Clone, PartialEq)]
pub enum CodeGenError {
    Unsupported(String),
    LoweringError(String),
    LlvmError(String),
    IoError(String),
    LinkError(String),
}

#[derive(Debug, Clone, PartialEq)]
pub enum LinkError {
    LinkerNotFound,
    LinkFailed(String),
    UndefinedSymbol(String),
    DuplicateSymbol(String),
}
```

## 11.3 LLVM 后端

LLVM 后端是幻语的主要原生代码生成后端，通过 `inkwell` crate 与 LLVM 交互。该后端支持 x86、ARM、RISC‑V 等所有 LLVM 目标平台。

### 11.3.1 后端结构

```rust
use inkwell::context::Context;
use inkwell::module::Module;
use inkwell::builder::Builder;
use inkwell::values::{BasicValueEnum, FunctionValue, PointerValue};
use inkwell::targets::{Target, TargetMachine, FileType, RelocMode, CodeModel};

pub struct LLVMBackend<'ctx> {
    context: &'ctx Context,
    module: Module<'ctx>,
    builder: Builder<'ctx>,
    target_machine: TargetMachine,
    options: CodeGenOptions,
    current_function: Option<FunctionValue<'ctx>>,
    variables: HashMap<String, PointerValue<'ctx>>,
    type_cache: HashMap<Type, BasicTypeEnum<'ctx>>,
}
```

### 11.3.2 类型转换

后端需要将幻语语义类型映射到 LLVM 类型：

```rust
impl<'ctx> LLVMBackend<'ctx> {
    fn convert_type(&mut self, ty: &Type) -> Result<BasicTypeEnum<'ctx>, CodeGenError> {
        if let Some(cached) = self.type_cache.get(ty) {
            return Ok(*cached);
        }
        let llvm_ty = match ty {
            Type::Int => self.context.i64_type().into(),
            Type::I8 => self.context.i8_type().into(),
            Type::I32 => self.context.i32_type().into(),
            Type::F64 => self.context.f64_type().into(),
            Type::Bool => self.context.bool_type().into(),
            Type::String => {
                let ptr = self.context.i8_type().ptr_type(AddressSpace::default());
                let len = self.context.i64_type();
                self.context.struct_type(&[ptr.into(), len.into()], false).into()
            }
            Type::List(inner) => {
                let ptr = self.context.i8_type().ptr_type(AddressSpace::default());
                let int = self.context.i64_type();
                self.context.struct_type(&[ptr.into(), int.into(), int.into(), int.into()], false).into()
            }
            Type::Ptr(inner) => {
                let inner_ty = self.convert_type(inner)?;
                inner_ty.ptr_type(AddressSpace::default()).into()
            }
            // 其他类型映射 ...
            _ => return Err(CodeGenError::Unsupported(format!("类型 {:?} 不直接支持", ty))),
        };
        self.type_cache.insert(ty.clone(), llvm_ty);
        Ok(llvm_ty)
    }
}
```

### 11.3.3 函数编译

函数编译的伪代码流程：

```rust
fn compile_function(&mut self, func: &Function) -> Result<FunctionValue<'ctx>, CodeGenError> {
    let return_ty = self.convert_type(&func.return_type)?;
    let param_tys: Vec<BasicTypeEnum> = func.params.iter()
        .map(|(_, ty)| self.convert_type(ty))
        .collect::<Result<_, _>>()?;

    let fn_type = match return_ty {
        BasicTypeEnum::IntType(t) => t.fn_type(&param_tys, false),
        BasicTypeEnum::FloatType(t) => t.fn_type(&param_tys, false),
        _ => self.context.void_type().fn_type(&param_tys, false),
    };

    let function = self.module.add_function(&func.name.name, fn_type, None);
    self.current_function = Some(function);

    let entry = self.context.append_basic_block(function, "entry");
    self.builder.position_at_end(entry);
    self.variables.clear();

    // 分配参数
    for (i, (name, _)) in func.params.iter().enumerate() {
        let param = function.get_nth_param(i as u32).unwrap();
        let alloca = self.builder.build_alloca(param.get_type(), &name.name)?;
        self.builder.build_store(alloca, param)?;
        self.variables.insert(name.name.clone(), alloca);
    }

    // 编译函数体
    for stmt in &func.body {
        self.compile_stmt(stmt)?;
    }

    // 确保有终止指令
    self.ensure_terminator(&func.return_type)?;
    self.current_function = None;
    Ok(function)
}
```

语句和表达式的编译方法 `compile_stmt`、`compile_expr` 按照 AST 节点递归生成对应的 LLVM IR 指令，包括算术运算、比较、条件分支、循环、函数调用等。

### 11.3.4 对象文件输出与链接

```rust
pub fn write_object_file(&self, path: &str) -> Result<(), CodeGenError> {
    self.target_machine
        .write_to_file(&self.module, FileType::Object, path.as_ref())
        .map_err(|e| CodeGenError::LlvmError(e.to_string()))
}
```

链接通常由系统链接器完成，编译器可以调用 `cc`/`lld` 等工具。

```rust
pub fn link_objects(objects: &[PathBuf], output: &Path, triple: &str) -> Result<(), LinkError> {
    let linker = if triple.contains("windows") { "link.exe" } else { "cc" };
    let mut cmd = std::process::Command::new(linker);
    for obj in objects { cmd.arg(obj); }
    cmd.arg("-o").arg(output);
    // 添加运行时库
    cmd.arg("-lhuanrt");
    let status = cmd.status().map_err(|e| LinkError::LinkFailed(e.to_string()))?;
    if status.success() { Ok(()) } else { Err(LinkError::LinkFailed("链接器退出码异常".into())) }
}
```

### 11.3.5 JIT 执行

幻语通过 LLVM JIT 提供即时执行能力，主要用于 REPL 和快速脚本运行。

```rust
pub struct JitEngine<'ctx> {
    execution_engine: ExecutionEngine<'ctx>,
}

impl<'ctx> JitEngine<'ctx> {
    pub fn new(module: Module<'ctx>) -> Result<Self, CodeGenError> {
        let execution_engine = module
            .create_jit_execution_engine(inkwell::OptimizationLevel::None)
            .map_err(|e| CodeGenError::LlvmError(e.to_string()))?;
        Ok(Self { execution_engine })
    }

    pub fn run_function<F, T>(&self, name: &str) -> Result<T, CodeGenError> {
        unsafe {
            let func: JitFunction<F> = self.execution_engine
                .get_function(name)
                .map_err(|e| CodeGenError::LlvmError(e.to_string()))?;
            Ok(func.call())
        }
    }
}
```

## 11.4 WebAssembly 后端

Wasm 后端复用 LLVM 的 Wasm 目标，支持 `wasm32-unknown-unknown` 等三元组。主要特性包括：

- 导出幻语函数（通过 `@导出` 属性）为 Wasm 导出
- 导入 JavaScript 函数（通过 `外部 "JavaScript"` 块）
- 支持 WASI 系统接口
- 提供 Wasm 专用优化选项（`--wasm-opt`）

```rust
pub struct WasmBackend {
    target_triple: TargetTriple,
    options: CodeGenOptions,
    llvm_backend: Option<LLVMBackend<'static>>,
}
```

其实现通过调用 LLVM 后端，仅目标三元组固定为 Wasm 系列，并附加 Wasm 特定的元数据生成。

## 11.5 GPU 后端

GPU 后端支持将标记为 `@内核` 的并行函数转换为 CUDA/HIP/OpenCL 代码。模块尚在早期开发中，计划提供以下接口：

```rust
pub struct GpuBackend {
    target: GpuTarget,
    options: CodeGenOptions,
}

pub enum GpuTarget { Cuda, Hip, OpenCL, Vulkan }
```

生成代码时，编译器会提取内核函数，生成对应的设备代码（如 PTX 或 SPIR‑V），并与主机端加载代码组合。

## 11.6 调试信息生成

LLVM 后端可通过 LLVM 的 DIBuilder 生成 DWARF 或 CodeView 调试信息，支持源码级断点、变量查看等。生成调试信息的开关由 `CodeGenOptions.debug_info` 控制。

---

## 本章总结

本章详细描述了幻语后端代码生成接口的完整设计，包括：

1. **后端概述**：LLVM后端、WebAssembly后端和GPU后端的完整架构
2. **CodeGenerator Trait**：统一的代码生成器接口定义
3. **代码生成选项**：OptLevel、CallingConvention等完整选项定义
4. **目标三元组**：TargetTriple和Arch枚举定义多架构支持
5. **LLVM后端**：使用inkwell crate与LLVM交互的完整实现
6. **WebAssembly后端**：Wasm字节码生成的完整支持
7. **GPU后端**：CUDA/HIP/OpenCL内核代码生成的计划接口
8. **调试信息生成**：DWARF/CodeView调试信息的完整生成支持

这些规范为幻语编译器的后端代码生成提供了完整的实现指导。

