# 基础项目库

// 导出函数
导出 函数 问候(名称: 字符串) -> 字符串 {
    返回 "你好, " + 名称 + "!"
}

// 导出结构体
导出 结构体 人员 {
    名称: 字符串,
    年龄: 整数,
}

实现 人员 {
    函数 新(名称: 字符串, 年龄: 整数) -> 人员 {
        返回 人员 { 名称, 年龄 }
    }

    函数 介绍() -> 字符串 {
        返回 "我是 " + 自我.名称 + ", 今年 " + 自我.年龄.转换为字符串() + " 岁"
    }
}
