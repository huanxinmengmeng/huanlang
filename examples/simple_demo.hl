# 简单幻语程序示例
# 用于验证 LLVM 后端编译功能

函数 主() 返回 整数 {
    变量 a: 整数 = 10
    变量 b: 整数 = 20
    变量 c: 整数 = a + b

    若 c > 25 则
        显示("c 大于 25")
    否则
        显示("c 小于或等于 25")
    结束

    返回 c
}

函数 加法(甲: 整数, 乙: 整数) 返回 整数 {
    返回 甲 + 乙
}