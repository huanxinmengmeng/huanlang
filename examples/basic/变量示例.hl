# 变量与类型示例
# 展示幻语中的变量声明、类型指定和类型推导

函数 主() 返回 整数
开始
    -- 基本类型变量
    令 年龄 类型 整数 为 25
    令 温度 类型 浮点64 为 36.5
    令 姓名 类型 字符串 为 "张三"
    令 是否成年 类型 布尔 为 真
    
    -- 类型推导
    令 分数 为 95        -- 推导为 整数
    令 圆周率 为 3.14159  -- 推导为 浮点64
    令 消息 为 "成功"     -- 推导为 字符串
    
    -- 常量
    定 最大重试次数 为 3
    定 应用名称 类型 字符串 为 "幻语应用"
    
    -- 输出
    显示("姓名: {}, 年龄: {}", 姓名, 年龄)
    显示("温度: {:.1}℃", 温度)
    显示("分数: {}, 是否成年: {}", 分数, 是否成年)
    显示("最大重试次数: {}", 最大重试次数)
    显示("应用名称: {}", 应用名称)
    
    返回 0
结束
