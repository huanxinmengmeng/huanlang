#!/usr/bin/env huan

# 幻语 Hello World 示例
# 这是一个最简单的幻语程序，展示基本的输出功能

导入 幻语.核心 为 核心

函数 主() 返回 整数
开始
    # 输出文本到控制台
    显示("你好，世界！")
    显示行("欢迎使用幻语编程语言")
    
    # 变量使用
    令 姓名 为 "幻语开发者"
    显示("你好，{}！", 姓名)
    
    # 返回状态码
    返回 0
结束
