# 应用程序

导入 core
导入 utils

函数 主() {
    // 使用核心模块
    让 响应 = core.发送请求("https://example.com")
    打印("网络请求结果:", 响应)

    // 使用工具模块
    让 数据 = { "名称": "测试", "值": 123 }
    让 序列化数据 = utils.序列化对象(数据)
    打印("序列化结果:", 序列化数据)

    打印("应用程序运行成功!")
}
