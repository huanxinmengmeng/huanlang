# 计算阶乘的例子
# 演示函数调用和返回值

# 阶乘函数
函数 阶乘(n -> 整数32) -> 整数32
开始
    若 n <= 1 则
        返回 1
    否则
        返回 n * 阶乘(n - 1)
    结束
结束

函数 主() -> 整数32
开始
    打印("计算阶乘!")
    打印("")
    
    # 测试几个数的阶乘
    打印("阶乘(5) =")
    打印(阶乘(5))
    
    打印("阶乘(7) =")
    打印(阶乘(7))
    
    返回 0
结束
